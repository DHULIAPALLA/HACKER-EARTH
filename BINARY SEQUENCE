def ispossible(x,y,a,b):
    if x*y==a+b:
        print("Yes")
    else:
        print("No")
n=int(input())
l=[]
for i in range(n):
    v=list(map(int,input().split()))
    l.append(v)
for i in range(len(l)):
    x=l[i][0]
    y=l[i][1]
    a=l[i][2]
    b=l[i][3]
    ispossible(x,y,a,b)
